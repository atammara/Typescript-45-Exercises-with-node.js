"use strict";
let user = ['sana', 'babar', 'saba', 'taha', 'admin'];
if (user.length === 0) {
    console.log("We need to find some users!");
}
else {
    user = [];
    console.log("All user names have been removed " + user.length);
}
